package com.example.taskkeeper.networking.spaceone

import com.example.taskkeeper.networking.spaceone.model.TaskItemSpo
import com.example.taskkeeper.utils.Constants
import retrofit2.Response
import retrofit2.http.GET
import retrofit2.http.Header
import retrofit2.http.Path

interface SpoApi {

    /**
     * @param {scopeKey} represents the name of the scope
     * @param {typeKey} represents the name of the item type
     */
//    @GET("/api/scope/{scopeKey}/itemtable/{typeKey}")
//    suspend fun getItemsList(
//        @Header(Constants.HEADER_AUTHORIZATION) authKey: String,
//        @Path("scopeKey") scopeKey: String,
//        @Path("typeKey") typeKey: String
//    ) : Response<List<TaskItemSpo>>


}